<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

    </data>

    <androidx.appcompat.widget.LinearLayoutCompat
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="@dimen/_5sdp"
        android:paddingVertical="@dimen/_3sdp"
        app:selector_background="@{@drawable/focus_unfocus_selector_background}">

        <com.dhiman.iptv.util.custom_view.HelveticaRegularTextView
            android:id="@+id/channelCategoryName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="@string/app_name"
            android:textColor="@color/white"
            android:textSize="@dimen/_8ssp"
            tools:context=".activity.disclaimer.DisclaimerActivity" />

    </androidx.appcompat.widget.LinearLayoutCompat>

</layout>